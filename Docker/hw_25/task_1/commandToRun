--Собрать образ
docker build -t my_webapp .

--запустить контейнер из собранного образа
docker run -d -p 8080:8080 --name myapp1 my_webapp

--на хостовой машине в браузере по http://localhost:8080/ отдает информацию из статичного файла